Place real external datasets in this folder.

Expected files:

- `kalro_siaya_soil.csv`
- `siaya_market_prices.csv` (optional)

The training pipeline will create a starter `kalro_siaya_soil.csv` template automatically if it is missing.
